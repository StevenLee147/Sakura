如果出现错误就修改错误，不要再禁用任何功能，不要进行任何简化。无论在任何的调试或测试过程中，都不要禁用原应该存在的代码。如果他们导致或可能导致报错，请根据报错信息进行修改，而非删除他们。出现函数调用错误时，先检查工作区内所有自定义的.h文件；创建函数时，先检查工作区内是否有功能类似的函数可以使用。允许你对工作区内的所有文件进行修改等操作。
编译时优先查找tasks.json
定义函数，变量，实现功能时先查看工作区下自定义的include文件夹（如sinclude），如果有功能类似的头文件，则将该函数，变量，功能放置在相应的头文件中。
按照提示创建新功能时，先查看工作区内是否有功能类似的实现可以复用，并保障新版本包含原版本的所有功能（除了我要求删除的）